type Book = {
    id?: number,
    title: string,
    author: string,
    isbn: string,
    published_year: string,
    genre?: string,
    available: boolean,
    shelf_location: string,
    subject: string,
}