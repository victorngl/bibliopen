type Item = {
    id?: number,
    isbn: string,
    title: string,
    published_year: string,
    genre?: string,
    edition?: string,
    language: string,
    physical_description?: string,
    general_notes?: string,
    bibliography_notes?: string,
    author: string[],
    subject: string[],
    dewey?: string,
    marc?: string,
}

